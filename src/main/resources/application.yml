# application.yml
spring:
  profiles:
    group:
      local:
        - cloud
        - localjwt
      prod:
        - cloud
      test:
        - localjwt

    active: local
  servlet:
    multipart:
      max-file-size: 10MB
