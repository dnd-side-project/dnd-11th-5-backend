spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: sa
    password:
    url: jdbc:h2:mem:~/odiga
    jpa:
      show-sql: true
      properties:
        hibernate:
          format_sql: true
      hibernate:
        ddl-auto: update
